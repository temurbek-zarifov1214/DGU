<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme (kept for library compatibility) -->
    <style name="Base.Theme.ManualApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/grey</item>
    </style>

    <!-- Compose activity theme — transparent bars, no action bar -->
    <style name="Theme.ManualApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
